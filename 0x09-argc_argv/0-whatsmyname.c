#include "stdio.h"

/**
 * main - prints file name
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 if successful
 */

int main(int argc, char **argv)
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
