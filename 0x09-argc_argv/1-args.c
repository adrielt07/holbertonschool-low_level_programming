#include "stdio.h"

/**
 * main - prints how many arguments are passed
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 if successful
 */

int main(int argc, char **argv)
{

	int n = 0;

	if (argv[n > 1])
		printf("%d\n", argc - 1);
	return (0);

}
